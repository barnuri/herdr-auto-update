{
    "name": "herdr-auto-update",
    "version": "0.1.0",
    "description": "Keep herdr always up to date — auto-applies patch/minor/major updates (each toggleable, all on by default) with live handoff",
    "author": "barnuri",
    "license": "MIT",
    "type": "commonjs",
    "engines": {
        "node": ">=22.0.0"
    },
    "scripts": {
        "test": "node --test \"tests/*.test.js\""
    },
    "keywords": ["herdr", "herdr-plugin", "auto-update", "updater"]
}
